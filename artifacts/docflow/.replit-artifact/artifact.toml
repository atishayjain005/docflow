kind = "web"
previewPath = "/"
title = "DocFlow"
description = "A lightweight collaborative document workspace for creating, formatting, importing, saving, and sharing internal documents with a small team."
version = "1.0.0"
id = "artifacts/docflow"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25803

[services.development]
run = "pnpm --filter @workspace/docflow run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/docflow", "run", "build" ]
serve = "static"
publicDir = "artifacts/docflow/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25803"
BASE_PATH = "/"
